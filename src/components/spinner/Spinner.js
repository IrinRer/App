import spin from './img/spin.svg';

const Spinner = () => {
    return (
        <img src={spin} style={{display: 'block',
        margin: 'auto',
        marginTop: '100px'}}/>
    )
}

export default Spinner;